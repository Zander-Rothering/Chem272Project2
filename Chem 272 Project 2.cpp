#include <vector>
#include <iostream>
#include <random>
#include "matplotlibcpp.h"

namespace plt = matplotlibcpp;

int main() {
std::random_device rd; // Provides a non-deterministic seed
std::mt19937 gen(rd()); // Mersenne Twister engine seeded by random_devic

Xinit = std::uniform_real_distribution<double> dist(0.0, 1.0);
Yinit = std::uniform_real_distribution<double> dist(0.0, 1.0);
mass = std::uniform_real_distribution<double> dist(0.0, 1.0);
    





    return 0;
}




